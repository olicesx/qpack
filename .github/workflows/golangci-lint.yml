name: golangci-lint
on:
  push:
    tags:
      - v*
    branches:
      - master
      - fix/typed-decode-errors
  pull_request:
  workflow_dispatch:

jobs:
  golangci:
    strategy:
      fail-fast: false
      matrix:
        go: [ "1.24.x", "1.25.x", "1.26.x" ]
    name: lint
    runs-on: ubuntu-latest
    env:
      GOLANGCI_LINT_VERSION: v2.11.4
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go }}
      - name: Install golangci-lint (built with this Go version)
        run: go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@${{ env.GOLANGCI_LINT_VERSION }}
      - name: Assert linter Go version >= module Go version
        # Fail closed: a linter that's too old for the module's Go version would
        # silently reduce analysis fidelity. Separate "tool too old" from
        # "code violates the linter".
        run: |
          set -euo pipefail
          want="go$(awk '/^go /{print $2; exit}' go.mod)"
          got="$(golangci-lint version 2>&1 | sed -n 's/.*built with \(go[0-9.]*\).*/\1/p')"
          if [ -z "$got" ]; then
            echo "::error::could not determine the Go version golangci-lint was built with"
            exit 1
          fi
          if [ "$(printf '%s\n%s\n' "$want" "$got" | sort -V | head -1)" != "$want" ]; then
            echo "::error::golangci-lint built with $got < module $want (use install-mode: goinstall)"
            exit 1
          fi
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          install-mode: goinstall
          version: ${{ env.GOLANGCI_LINT_VERSION }}
